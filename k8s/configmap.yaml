apiVersion: v1
kind: ConfigMap
metadata:
  name: employee-api-config
  namespace: default
data:
  # Change this value to switch database providers: SqlServer | PostgreSQL | MySQL
  DatabaseProvider: "SqlServer"
  ASPNETCORE_ENVIRONMENT: "Production"
